const { execSync } = require('child_process');

async function testApi() {
  try {
    // Read the test user's data from web application's localstorage somehow...
    // Let's use curl against the api to try manually authenticating as org admin if there is one available
  } catch (error) {
    console.error(error);
  }
}

